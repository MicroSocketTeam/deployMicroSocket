package main

import (
	"fmt"
	"os"
	"os/signal"
)

func main() {
	//c := make(chan os.Signal, 1)
	//signal.Notify(c, os.Interrupt, os.Kill)
	//
	//who := "Alice "
	//if len(os.Args) != 1 {
	//	log.Printf("%v", "错误的cli参数(Missing cli parameters)")
	//}
	//fmt.Println("Good Morning", who)
	//
	//
	//sign := <-c
	//log.Println("已退出microSocket框架(Quit the microSocket framework.),信号量:",sign)


}
